// components/authority/VehicleDetailsPanel.tsx - DISABLED

import React from 'react';

// This component is now disabled as per the new design.
const VehicleDetailsPanel: React.FC<{ vehicle: any; onClose: () => void; }> = () => {
    return null;
};

export default VehicleDetailsPanel;
                            